/** Re-export for legacy imports — use `@/types/orders` in new code. */
export type {
  PdfFoodCostExtra,
  PdfOrder,
  PdfOrderDocumentData,
  PdfOrderLineItem,
  PdfOrderSubItem,
} from "@/types/orders";
