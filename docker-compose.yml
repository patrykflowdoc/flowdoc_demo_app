name: szczypta-smaku

services:
  seed:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: builder
    environment:
      - DATABASE_URL=postgresql://postgres:test@postgres:5432/szczypta-smaku?schema=public
    command: npx prisma db seed
    depends_on:
      - postgres
    networks:
      - szczypta-smaku
    profiles:
      - tools   # optional: only start with docker compose --profile tools run seed
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=test
      - POSTGRES_DB=szczypta-smaku
      - LANG=C.UTF-8
      - LC_ALL=C.UTF-8
    ports:
      - "5431:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - szczypta-smaku
  backend:
    image: szczypta-smaku-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "25044:25044"
    environment:
      - DATABASE_URL=postgresql://postgres:test@postgres:5432/szczypta-smaku?schema=public
    volumes:
      - ./backend:/app
      - /app/node_modules  # zachowaj node_modules z obrazu (zawiera dotenv itd.)
    networks:
      - szczypta-smaku
    depends_on:
      - postgres
  catering:
    image: szczypta-smaku-catering
    build:
      context: ./catering
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - VITE_API_PROXY_TARGET=http://backend:25044
    volumes:
      - ./catering:/app
      - /app/node_modules # wlasny node_modules
    networks:
      - szczypta-smaku
    depends_on:
      - backend
  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"   # HTTP - wymagany dla weryfikacji Let's Encrypt
      - "443:443" # HTTPS
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data      # Przechowuje certyfikaty SSL
      - caddy_config:/config  # Konfiguracja Caddy
    networks:
      - szczypta-smaku
    depends_on:
      catering:
        condition: service_started
      backend:
        condition: service_started

volumes:
  caddy_config:
  caddy_data:
  postgres_data:
networks:
  szczypta-smaku:
    driver: bridge