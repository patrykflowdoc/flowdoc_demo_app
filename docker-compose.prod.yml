# Production override: no source mounts — use built images only.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

name: szczypta-smaku

services:
  backend:
    volumes: []
  catering:
    volumes: []
