# Production override: no source mounts — use built images only.
# Usage: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
#
# Nadpisuje nazwę projektu z docker-compose.yml (dev: szczypta-smaku-local).

name: flowdoc-demo

services:
  backend:
    volumes: []
  catering:
    volumes: []
  # W produkcji nie wystawiaj Postgresa na host — dostęp tylko z sieci Dockera.
  postgres:
    ports: []
