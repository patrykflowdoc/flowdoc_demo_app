/**
 * Seven-days notifier is not used in this project.
 * Stub kept for compatibility if called from cron/scheduler.
 */
export const sevenDaysNotifier = async () => {
    return { sent: 0, failed: 0, errors: [] };
};
